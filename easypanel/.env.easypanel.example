# ============================================
# MySQL Optimizer - Easypanel Environment
# ============================================
# คัดลอกเป็น .env ที่ root ของโปรเจกต์
# แล้วแก้ค่าตามเซิร์ฟเวอร์จริง
# ============================================

# ====================
# PostgreSQL Database
# ====================
POSTGRES_DB=mysql_optimizer
POSTGRES_USER=optimizer_user
POSTGRES_PASSWORD=CHANGE_ME_strong_password_here

# ====================
# Authentication
# ====================
# ใช้ openssl rand -base64 32 เพื่อสร้างค่าแบบสุ่ม
JWT_SECRET=CHANGE_ME_jwt_secret_at_least_32_chars
API_SECRET=CHANGE_ME_api_secret_at_least_32_chars

# Tenant ID (ใช้ค่า default หรือสร้างใหม่)
TENANT_ID=00000000-0000-0000-0000-000000000000

# ====================
# Admin UI Login
# ====================
ADMIN_USERNAME=admin
ADMIN_PASSWORD=CHANGE_ME_admin_password

# ====================
# URLs (สำคัญมาก!)
# ====================
# NEXT_PUBLIC_API_BASE_URL = URL ที่ browser จะเรียก API
# - ต้องเป็น URL สาธารณะที่เข้าถึงได้จากอินเทอร์เน็ต
# - ถ้าใช้ Easypanel domain: https://your-app-saas-api.easypanel.host
# - ถ้าใช้ custom domain: https://api.yourdomain.com
NEXT_PUBLIC_API_BASE_URL=https://your-api-domain.com

# CORS_ORIGIN = URL ของ Admin UI (ให้ API รับ requests)
# - ถ้าใช้ Easypanel domain: https://your-app-admin-ui.easypanel.host
# - ถ้าใช้ custom domain: https://app.yourdomain.com
# - ใช้ * ถ้าไม่แน่ใจ (ไม่แนะนำสำหรับ production)
CORS_ORIGIN=https://your-ui-domain.com

# ====================
# Optional: Agent Config
# ====================
# ถ้าต้องการเพิ่ม Agent ทีหลัง ใช้ค่าเหล่านี้
# AGENT_MODE=both
# AGENT_ID=agent-prod-1
# MYSQL_TARGET_HOST=your-mysql-server.com
# MYSQL_TARGET_PORT=3306
# MYSQL_TARGET_USER=optimizer
# MYSQL_TARGET_PASSWORD=mysql_password
# MYSQL_TARGET_DATABASE=your_database
